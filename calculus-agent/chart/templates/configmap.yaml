apiVersion: v1
kind: ConfigMap
metadata:
  name: {{ include "calculus-agent.fullname" . }}-config
  labels:
    {{- include "calculus-agent.labels" . | nindent 4 }}
data:
  {{- range $key, $value := .Values.config }}
  {{ $key }}: {{ $value | quote }}
  {{- end }}
