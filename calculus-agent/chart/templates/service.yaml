apiVersion: v1
kind: Service
metadata:
  name: {{ include "calculus-agent.fullname" . }}
  labels:
    {{- include "calculus-agent.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "calculus-agent.selectorLabels" . | nindent 4 }}
