apiVersion: v1
kind: Service
metadata:
  name: {{ include "calculus-gateway.fullname" . }}
  labels:
    {{- include "calculus-gateway.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "calculus-gateway.selectorLabels" . | nindent 4 }}
