apiVersion: v1
kind: Service
metadata:
  name: {{ include "calculus-ui.fullname" . }}
  labels:
    {{- include "calculus-ui.labels" . | nindent 4 }}
spec:
  type: ClusterIP
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "calculus-ui.selectorLabels" . | nindent 4 }}
